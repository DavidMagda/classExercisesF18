#include <iostream>
using namespace std;

int main()
{
    int points_possible = 0;
    ;
    int points_earned = 10;
    
    if (points_possible > 0 && points_earned / points_possible >= 0.60)
    {
   cout << "You pass.";
    } 

    else 

    {
        
      cout << "You fail.";
      
    }

return 0;
   
}
